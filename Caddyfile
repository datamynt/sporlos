# Auto-HTTPS via Let's Encrypt. SPORLOS_DOMAIN settes i .env.
{$SPORLOS_DOMAIN} {
	reverse_proxy app:8000
}

# www → apex (eget sertifikat hentes automatisk)
www.{$SPORLOS_DOMAIN} {
	redir https://{$SPORLOS_DOMAIN}{uri} permanent
}
